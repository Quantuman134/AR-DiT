# This file exists only to keep the secrets/ directory tracked in git.
# Drop your wandb token here as `secrets/wandb.token` — its contents are
# gitignored by the sibling `.gitignore` rules. See README.md → Install
# → Weights & Biases for details.
